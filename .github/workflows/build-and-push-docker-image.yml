name: 'Build and Push Docker Image'
on:
  workflow_dispatch:
  schedule:
    # weekly
    - cron: '0 0 * * 0'

jobs:
  check-release:
    runs-on: ubuntu-latest
    outputs:
      target-release: ${{ steps.mhsendmail.outputs.release }}
      self-release: ${{ steps.self.outputs.release }}
    steps:
      - id: mhsendmail
        uses: pozetroninc/github-action-get-latest-release@v0.5.0
        with:
          owner: mailhog
          repo: mhsendmail
          excludes: prerelease, draft
      - id: self
        uses: pozetroninc/github-action-get-latest-release@v0.5.0
        with:
          repository: ${{ github.repository }}

  build-and-push:
    runs-on: ubuntu-latest
    needs:
      - check-release
    if: ${{ needs.check-release.outputs.target-release }} != ${{ needs.check-release.outputs.self-release }}
    steps:
      - uses: actions/checkout@v3
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v3
        with:
          push: true
          tags: tktcorporation/mhsendmail:latest, tktcorporation/mhsendmail:${{ needs.check-release.outputs.target-release }}
          build_args: MHSENDMAIL_VERSION=${{ needs.check-release.outputs.target-release }}
